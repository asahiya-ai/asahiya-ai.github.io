.env
.env.*
!.env.example
*.key
*.pem
secrets.json
node_modules/

# 動画・音声の原本（1本40MB超）。gitに入れると履歴が永久に膨らむため除外。
# ローカルには残っている。コピーは assets/raw/ にもある。
*.mp4
*.mov
*.mp3
*.wav

# サイトの素材ではない作業ファイル（このリポは GitHub Pages で公開されるので、`git add .` で上がらないようにする）
# 本来の置き場は asahiya/CLAUDE.md §5（提案書→consulting/teian、素材→assets/raw）
/セミナー/
/動画・広報/
*.docx
*.pptx
/*.jpg
/*.png
.claude/
